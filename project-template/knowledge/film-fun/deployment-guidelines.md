# film.fun Deployment Guidelines

- Deploy contract producers before strict consumers when introducing new fields.
- Use phased rollout and feature flags for shared contract migrations.
- Keep rollback path per deployment target.
